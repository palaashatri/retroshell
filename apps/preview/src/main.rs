//! Preview — SLOPOS image viewer.
//!
//! Shows images from the command line or via the Finder, and exposes vision
//! actions (extract text, lift subject) through the SLOPOS Vision daemon.

fn main() {
    log::warn!("preview is not yet implemented");
}
