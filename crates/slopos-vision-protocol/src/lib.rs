//! SLOPOS Vision protocol — shared request/response types between the
//! `slopos-visiond` daemon and its clients.
//!
//! Kept dependency-free (serde + serde_json only) so both sides can depend on
//! it without pulling in the inference runtime.

// Placeholder; request/response types land with the daemon (V2).
