# Docs

**Single living source of truth:** **[SLOPOS-I.md](SLOPOS-I.md)**

Everything else under `docs/` is evidence (`qa/`), executors (`tasks/`),
long-form history (`specs/`), or archive. Do not add new parallel SoT files.

Former split docs (`PROGRAM`, `UI`, `HANDOFF`, `FUTURE`, `MATURITY`) live in
[`archive/living-split-2026-07-31/`](archive/living-split-2026-07-31/) and are
superseded.
