# v0.2.0 Progress Report

**Session Date:** July 31, 2026  
**Status:** Bare-minimum v0.2.0 functionality visually proven on UTM VM

## Honesty correction

Previous session artifacts claimed Spotlight/theme “complete” and QA “passed” while:
- Spotlight widgets were never in the canvas paint tree
- Kit `Widget::draw` stubs paint nothing
- Screenshots were blank 263-byte PNGs

That is retracted. Current evidence is only `docs/qa/v0.2.0/*.png` + this report.

## Done (proven)

### Spotlight actually paints
- Added `retro_kit::Panel` (scrim/card) + SDK `draw_widget` branch
- Spotlight widgets (`scrim`, `card`, `search_field`, `results_list`) are real `ShellDesktop` children when visible
- Removed false `draw_overlay()` that called empty kit `draw()` methods
- QA screenshots: `02-spotlight.png`, `03-spotlight-empty.png`, `05-spotlight-settings.png`

### Theme preference affects pixels
- `theme=dark` in `~/.config/retroshell/settings.conf` → `04-desktop-dark.png`
- Light desktop → `01-desktop.png`

### VM visual capture path restored
- sway headless + grim (not ImageMagick `import` on empty Xvfb)
- Recipe documented in `docs/qa/v0.2.0/QA-RESULTS.md`

### Tests
- 317 lib tests passing on VM (`retro-kit` / `retro-sdk` / `retro-shell`)

## Not done / not claimed

- Defect J button clicks **not re-run** on UTM this session
- Live Settings theme toggle UI not screenshotted
- App icon placeholders still shared
- Release tag not cut yet (awaiting user OK after reviewing screenshots)

## Files touched

- `crates/retro-kit/src/panel.rs` (new)
- `crates/retro-kit/src/lib.rs`
- `crates/retro-sdk/src/lib.rs` (draw Panel)
- `crates/retro-shell/src/spotlight_ui.rs` (real overlay widgets)
- `crates/retro-shell/src/lib.rs` (children paint path, QA env hook)
- `docs/qa/v0.2.0/*`
